<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>雪线之上 | 高山人文纪实纪录片</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: "Times New Roman", "Noto Serif SC", serif;
      background: #f8f8f8;
      color: #333;
      line-height: 1.8;
    }

    /* 左侧固定导航 跟你旧网站一模一样 */
    .aside {
      position: fixed;
      left: 0;
      top: 0;
      width: 240px;
      height: 100vh;
      background: #fff;
      border-right: 1px solid #eee;
      padding: 60px 30px;
    }
    .aside-title {
      font-size: 24px;
      letter-spacing: 4px;
      color: #222;
      margin-bottom: 60px;
      font-weight: 500;
    }
    .nav-link {
      display: block;
      margin: 20px 0;
      color: #666;
      text-decoration: none;
      font-size: 15px;
      letter-spacing: 1px;
      transition: 0.3s;
    }
    .nav-link.active {
      color: #222;
      border-left: 2px solid #222;
      padding-left: 12px;
    }
    .nav-link:hover {
      color: #222;
    }

    /* 右侧主内容区 */
    .container {
      margin-left: 240px;
      padding: 80px 100px;
      max-width: 900px;
    }
    .page {
      display: none;
    }
    .page.show {
      display: block;
    }
    h2 {
      font-size: 28px;
      margin-bottom: 30px;
      letter-spacing: 2px;
      color: #222;
    }
    p {
      font-size: 16px;
      color: #555;
      margin-bottom: 20px;
      text-align: justify;
    }
  </style>
</head>
<body>

<!-- 左侧导航 -->
<div class="aside">
  <div class="aside-title">雪线之上</div>
  首页
  导演阐述
  影片简介
  主创团队
  合作联系
</div>

<!-- 右侧内容 -->
<div class="container">
  <!-- 首页 #home -->
  <div id="home" class="page show">
    <h2>雪线之上 · Above the Snow Line</h2>
    <p>高海拔高山人文纪实纪录片，行走青藏高原雪线之间，以镜头记录旷野、生灵与坚守。</p >
  </div>

  <!-- 导演阐述 #director -->
  <div id="director" class="page">
    <h2>导演阐述</h2>
    <p>以高山摄影师视角深入雪域腹地，坚持纪实本真，不刻意编排、不渲染情绪，只用最克制的镜头，记录雪线之上真实的人与生活。</p >
    <p>在海拔四千余米的旷野里，看见平凡人的坚韧，看见山野里的温柔，留存属于高原独有的人文印记。</p >
  </div>

  <!-- 影片简介 #intro -->
  <div id="intro" class="page">
    <h2>影片简介</h2>
    <p>本片聚焦高海拔雪山地域人文生态，跟随当地山野行者、高原女性群体，记录日常劳作、山野迁徙、信仰生活与世代传承。</p >
  </div>

  <!-- 主创团队 #team -->
  <div id="team" class="page">
    <h2>主创团队</h2>
    <p>导演 / 高山摄影 / 野外制片 / 后期剪辑 / 田野调研</p >
  </div>

  <!-- 合作联系 #contact -->
  <div id="contact" class="page">
    <h2>合作与影展投递</h2>
    <p>欢迎电影节申报、发行合作、媒体访谈、文创跨界对接。</p >
  </div>
</div>

<script>
// 实现 #director 这种网址跳转，和你旧网站完全一样
const links = document.querySelectorAll('.nav-link');
const pages = document.querySelectorAll('.page');

function changePage(pageId) {
  pages.forEach(p => p.classList.remove('show'));
  links.forEach(l => l.classList.remove('active'));
  document.getElementById(pageId).classList.add('show');
  document.querySelector(`[data-page="${pageId}"]`).classList.add('active');
  history.pushState(null, null, '#' + pageId);
}

links.forEach(link => {
  link.addEventListener('click', e => {
    e.preventDefault();
    changePage(link.dataset.page);
  });
});

// 打开网址自动识别 #director / #home
window.addEventListener('load', () => {
  let hash = location.hash.replace('#', '') || 'home';
  changePage(hash);
});
</script>

</body>
</html>
